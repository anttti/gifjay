{
  "name": "GifJay",
  "version": "0.0.1",
  "description": "A simple GIF viewer",
  "main": "gifjay.js",
  "author": "Antti Mattila",
  "license": "MIT",
  "dependencies": {
    "bluebird": "~2.3.2"
  },
  "devDependencies": {
  },
  "scripts": {
    "watch": "mkdir -p build && watchify src/gifjay.js -o build/bundle.js",
    "build": "mkdir -p build && browserify src/gifjay.js -o build/bundle.js",
    "clean": "rm -rf build/"
  }
}
